import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    gap: 10,
    alignItems: 'center',
    paddingBottom: 20,
  },
  containerTitle: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 10,
  },
})

export default styles
